import queue
import time

Score_queue = queue.Queue()

 def process_Score():
    while True:
    if not Score_queue.empty():
           addr, action = Score_queue_queue.get()
            print(f"Processing game logic from {addr}: {action}")
            time.sleep(1)  
            print(f"Game logic updated for {addr}")

logic_worker = threading.Thread(target=process_Score)
logic_worker.start()
